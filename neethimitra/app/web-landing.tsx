import React from 'react';
import { WebLandingPage } from '@/components/web/WebLandingPage';

/**
 * /web-landing — Public marketing route (web-only).
 * Expo Router serves this file as the /web-landing route.
 */
export default function WebLandingScreen() {
  return <WebLandingPage />;
}
